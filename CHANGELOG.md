# 变更日志

## 2026-07-27 v1.8（用户逐图检视 P21 查出 4 类缺陷，本体三处升级——全部按类型无关的通用形式表述）

- **红线「只标讲师讲过的」升级为落笔留证**（annotate-style 标定工序新增第 4、5 条 + 防呆第 5 条改写）。v1.3 红线设立后仍在 P21/P22 连续复发（外壳选项面板把讲师从没提过的「默认衬垫 25%」等参数标了出来，P21 转写稿搜「衬垫」零命中）——证明事后回想式核对挡不住"面板项就在那、标签又好放"。新机制：**每条标签落笔时写转写稿时间戳依据，写不出依据的不进 boxes**（下沉正文并注明「视频未展开讲解」）；防呆第 5 条改为核查依据齐全与否。同时新增标定第 4 条：**精读一张立刻写一张的配置**，杜绝批量精读后统一落笔造成的坐标串行（P22 domain-dims 事故）。
- **pipeline ③ 新增选帧双判据**：时刻命中 + 画面能独立传达这一步在干什么。三维对象的操作要三维透视帧（P21 圆柱切割抽成正对轴线的二维半圆，用户指出完全看不出三维操作）；理论课"半个公式的过渡帧"同理。
- **pipeline ⑥ 完整性对账加两条铁律**：① 视频最后 10% 单独再滚一遍——尾段（删除/改名/保存/总结）是系统性弱区，已三案并发（P21 删实体改名整段跳过、修复时衔接句又想当然写错、v1.6 总结回放）；② 操作链/推导链不因"旧知识复用"断链——链上老操作至少显式回指，值得就补图（P21 底面切割被压成一句话、图链断裂）。
- **pipeline ④ 组织轴扩成类型矩阵**（操作型/理论型/问答型 × 组织轴/选帧重心/标注重心/图文密度）。操作型列由 6 集实战填实；理论型/问答型两列只填有把握的默认值并显式标注「未实战验证」，待真实处理到该类型时按 lessons 机制长实——不凭空编造未验证的规则。
- 成品修复：P21 三图重做（外壳面板去衬垫标签、圆柱切割换三维透视帧、新增辅助平面切底面图 cyl-ground）+ cyl-final 加「废体已删」标注；P22 外壳面板图重做（去类型/衬垫标签）；两篇笔记正文同步修订。

## 2026-07-26 v1.7（跨目录调用暴露的规范缺口）

- **新增前置门槛：落 vault 但会话不在 vault 内时，先读 vault 根 `CLAUDE.md`**（SKILL.md「作用范围与前置确认」）。起因是 P21 这一跑发生在 `~/cc` 而非 vault 目录：`~/.claude/skills/video-notes` 是指向 `~/.cc-switch/skills/video-notes` 的软链，skill 本体两处完全同一份，产物（22 张图入 `raw/media/fluent-course/`、笔记入 `lesson/`、课程索引 + `cfd/_index` + `wiki/index.md` + `log.md` + `hot.md` 全部更新、零死链）与在 vault 内跑的 P19/P20 逐项对齐，**没有翻车**。但没翻车靠的是两个偶然：① `pipeline.md` ⑥ 把落点路径写得够全；② 该会话接续了前一个 vault 会话，上下文里已带着 vault 路径与规范。
- 缺口的实质：**cwd 不在 vault 时，vault 根 `CLAUDE.md` 不会被加载**，而 frontmatter 四硬字段、目录归位表、`log.md` 3 天/周/月/年四级折叠规则、`raw/` 只读纪律、结构演化触发条件这些只写在 vault 侧，`pipeline.md` ⑥ 一条都没有。冷启动在非 vault 目录跑就会漏。
- 判据刻意定为「**产物是否落 vault**」而非「cwd 是否等于 vault 根」：在非 vault 目录跑却输出平铺 `笔记.md + images/` 时，vault 规范用不上，读了是浪费；且 cwd 在 vault 子目录时 `CLAUDE.md` 本就会向上递归加载。
- 同轮讨论中另外两项（把 vault 绝对路径写死进 SKILL.md、门槛里连 `wiki/hot.md` 一起读）用户明确只要 `CLAUDE.md` 这一条，未采纳。

## 2026-07-26 v1.6（P21 跑出的三条，经用户批准入本体）

- **数值类目标帧改抽两个时间点**（pipeline.md ② + annotate-style.md 防呆第 2 条）。原策略是"念出数值时刻 +1~3s"，只覆盖了"拖动中间值"这一种失效；P21 撞上第二种——讲师口述"半径 3000"时压根还没开始输入，该秒帧上是软件默认值 977.93mm。归纳出统一根因：**讲师"说出"数值的时刻 ≠ 该值出现在界面的时刻，口述先于输入 5~20 秒**。新策略：凡转写稿出现具体数值的时间点，同时抽「口述时刻」与「口述后 10~20 秒」，一律以后者核对。此类坑累计三次。
- **新增防呆第 4 条：序号链里只放主线元素**（annotate-style.md，清单顺延为 1~9 条）。①②③ 会被读者默认成先后顺序，所以并列项、旁例、备选做法必须用无序号标签 +「另一规则 / 另一种做法」前缀与主链区分。P20 强力选择图把"圆角可批量选"这个旁例编进了序号链，用户看完图来问"为什么先选圆角再选凹陷"——这是防呆第 3 条的第三种失效形态（前两次是序号排错，这次是不该进链的进了链）。
- **明确 boxes 与 notes 的选用判据**（annotate-style.md 样式规范）。notes 原本只写了"云图/网格全景"，补上第二类典型：**操作结果展示图**（切完一半的模型、移动到位的边界、生成好的计算域）。判据一句话：能指到具体控件 → boxes；只能说"整张图现在是这个状态" → notes；同一集混用是正常的。

另新增 pipeline.md ② 一条：**主动找"总结回放"**。通读转写稿时留意「继续走这个工作流程 / 回顾一下 / 总结一下」这类措辞，讲师多半在放总结性课件——这类帧不在按操作步骤列的 outline 里、场景检测也只当普通切换，得手动补进目标帧清单。P21 的 SCDM 完整工作流程图就是补抽时意外撞见的，最终用作笔记第 0 节定位图。

## 2026-07-26 v1.5（用户否掉「术语校正表」这道自创工序）

- **新增质量红线：不罗列字幕转写错误**（SKILL.md）。P20 页尾我自造了一张「术语校正表（字幕 → 界面/正确写法）」，把 `space claim / 薛氏的面 / 直道` 等 7 条转写错误列出来对照。用户指出：**AI 字幕认错词是既定前提，读者按上下文自己就能还原**，专门列表属于噪音。今后笔记直接用正确写法行文，不留纠错痕迹。
- **保留的唯一例外**：讲师**口述**与界面文字确实不一致（讲师真说了那个词，不是转写错）。这种会让人跟着视频操作时找不到按钮，仍要提示，但措辞写成「讲解与界面不一致」而非字幕问题。P20 的"最小距离 vs 界面最短距离"即属此类，已按新措辞保留。
- 顺带修：P20 强力选择图的序号链把并列旁例（圆角）编了进去，被读成"先选圆角再选凹陷"的五步操作。已重渲为无序号旁注 + 主链四步，并在笔记补机制说明（种子+规则模型、边的凹凸性判据）。该条泛化记入 lessons.md，**尚未入本体，待用户拍板**。

清理范围核实：P19 / P24 两页无同类表格（该工序是 P20 独有的自创动作，流水线文档里本就没有），仅 P19 开头一句"字幕里被听错成 street claim…"按新规精简。删表前逐词确认 7 条术语在正文均有着落（SpaceClaim 3 次 / 缺失的面 7 次 / 剖面 4 次 等），知识零丢失。

## 2026-07-26 v1.4（P20 跑出的两条，经用户批准入本体）

- **contact_sheet.py 改自然排序**。原先用字典序，`p152.0s` 会排到 `p1510.0s` 之后，读候选帧总览时时间戳来回跳，既费眼又容易误判"某段没抽到"。新增 `natural_key()`：用 `re.split(r"(\d+)")` 切分后把数字段转 int 比较。做成**通用自然排序**而非专解析秒数，成品图名（`fluent-p20-*`）等非时间戳命名同样受益。类型安全有保证——re.split 使奇数位恒为数字段、偶数位恒为非数字段，同位置类型必然一致，不会 int 与 str 相比。
- **防呆清单第 3 条从"操作序"扩为"讲解序"**（annotate-style.md）。原措辞只覆盖操作图（动作顺序），P20 的车轮凸台图是**说明图**（无动作，只讲状态与因果），我按图上的视觉动线排成了 ①保留的凸台 ②轮毂已填平，读者会先读到例外再读到主干。新措辞分两类给判据：操作图＝动作顺序；说明图＝讲解顺序（先主干后例外、先结论后限定），并标注该条已二次复现。

验证：`py_compile` 通过；新旧排序对比确认时间戳单调递增（旧序 p1010→p1032→…→p112.5，新序 p112.5→p122→p126→…）；边界用例（数字开头 `152.png`、纯字母 `abc.png`、数字非数字交替 `p10x2.png`）均无 TypeError 且排序合理；`frames_pts`(111 张，拆 4 张表) 与 `shots`(21 张) 两个目录回归出图正常。另一条 lessons（细环目标红框易框到相邻大块区域）按约定留在 lessons.md，未入本体。

## 2026-07-26 v1.3（用户逐张审图查出的两类硬伤）

用户对 P19 成品逐张检视，指出 5 处问题，经批准把其中两条泛化入本体：

- **annotate.py 字形体检重做**。原自检 `getmask("中").getbbox()` 形同虚设——**.notdef（方框带叉）本身就有 bbox**，它只能检出"整个字体加载失败"，检不出"个别字符缺字形"。事故实况是两层失效叠加：回退链首位 `PingFang.ttc` 在本机已打不开（cannot open resource），实际落到 `Hiragino Sans GB`，而该简中字体不含 Ø(U+00D8)，于是静默渲染成方框带叉进了成品图。
  改动：新增 `has_glyph()`，用**私用区码点 U+E000 的渲染指纹**作 .notdef 基准做 md5 比对；`load_font()` 改用它判定并在全部候选失败时报出每个候选的具体失败原因；新增 `check_glyphs()`，在渲染前对配置里所有 label 与 notes **逐字符体检**，命中缺字形直接 raise 并列出「字符 + 码点 + 出现在哪几张图」，宁可不出图也不出带方框的成品。
- **SKILL.md 质量红线新增「只标讲师讲过的」**。P19 有 4 张图标注了视频只字未提的左侧选项面板（旋转选项/组合选项/移动选项/修剪选项）。成因写进了红线本身：那些面板恰好在帧里可见、又恰好占着大片空白好放标签，于是取图逻辑从"复现讲师讲了什么"滑向"这一帧能看到什么就标什么"。红框会被读者默认为讲师强调过的要点，标了等于凭空捏造重点。
- 防呆清单同步扩到 8 条：新增第 3 条「序号是操作序不是重要性序」（规范早有、实操最易忘）、第 4 条「标的是不是讲师讲过的」；第 5 条改写为说明新的字形体检行为与"不要用 getbbox 自行判断"。

验证：新版重跑 P19 的 7 张修正图，与改动前**逐字节一致**；反例（把 φ 换回 Ø 并塞入 ⌀ U+2300）被正确拦下——报错指名两个字符及各自出现的图，`out_bad/` 零产出。

## 2026-07-26 v1.2（P19 跑出的数值型核验漏洞）

经用户批准，把当日 1 条 lessons 泛化入本体（另 2 条留在 lessons.md 未动）：

- **防呆清单新增第 2 条「图内数值对不对」**（annotate-style.md）：凡标签引用了图内数值，必须全分辨率确认该数值真在图里。这是与既有第 1 条（UI 过渡瞬间）**不同的失效模式**——帧本身完好无撕裂，但演示"键入数值"的镜头里，输入框显示的是鼠标拖动的实时中间值，不是讲师最终键入的目标值。P19 实例：画圆标签写 152.4、图上是 21.26mm；拉动标签写 355.6、图上是 62.62mm。补抽策略写明为「按转写稿里念出数值的时刻 +1~3s」（讲师先念后输）。原第 2~5 条顺延为 3~6 条，第 5 条内部引用同步改为「见第 1、2 条」。
- **SKILL.md 质量红线同步扩容**：核验对象从"对话框/弹窗类关键帧"扩到"以及标签引用了图内数值的关键帧"，并记录两类失效模式各自的中招次数（过渡型 3 次 / 数值型 2 次）。

验证：P19 的 25 张成品即按新规则核验通过（两处数值型错帧在补抽阶段被拦下，未进笔记）。纯文档改动，脚本未动。

## 2026-07-24 v1.1（P24 opus4.8 重做版跑出的三处健壮性修复）

经用户批准，把当日 3 条 lessons 泛化入本体：

- **annotate.py 不再因放不下标签而崩溃**：`place()` 改返回 `(placed, warns)`，找不到空位时用新增的 `cheapest()` 降级为压盖放置并打印 `⚠️ 图名: 标签` 告警；主循环统计告警数。原行为是返回 `None` 后在 `render()` 里抛 `TypeError`，且不指明是哪张图。
- **annotate.py 异常带定位信息**：单张渲染包 try/except，重抛时带上输出图名与源帧名，几十张配置里可直接定位。
- **标签文字规范收紧**（annotate-style.md）：长度 ≤14 个全角字符；序数/口诀/推导一律下沉正文。理由：标签宽度直接决定落点，写长了会被扔到大片空白区、恰好盖住要数数的选项行（"模型第 4 项"把前 3 项挡住的实例）。防呆清单新增第 5 条"不得忽略压盖告警"。
- **contact_sheet.py 自建输出目录**；`--width` 帮助文字点明是单图宽而非画布宽。

回归验证：P24 的 34 张成品用新版重跑，与改动前**逐字节一致**；用当初崩溃的配置（超大红框 + 长标签）复现，现为告警 + 正常出图、退出码 0；异常路径与自建目录均实测通过。

## 2026-07-24 v1.0（首版，由 Fluent 课程 P24 试点固化）

- 六阶段流水线：获取（分 P 感知）→ 文字轨（平台字幕优先）→ 视觉轨（场景检测+定点补抽+间隔兜底）→ 理解对齐（类型定轴、帧校术语）→ 制图（v3 标注引擎）→ 落库校验（vault 规范/通用两套）。
- v3 标注样式定稿：细红框 + 红底白字标签智能避让（分块方差挑空白落点）+ 引线，不扩画布；样式经用户三轮验收（v1 压图、v2 只剩编号均被否）。
- 三脚本：extract_frames.py（三模式抽帧）、annotate.py（JSON 配置驱动标注引擎）、contact_sheet.py（总览粗筛/自检）。
- 自我优化机制：卡点即记（lessons.md append-only）→ 每集微复盘向用户提优化选项 → 教训泛化经用户批准入本体。
- 预置 6 条已验证教训（分 P 丢失、字幕优先、场景检测漏对话框、过渡帧、字体降级、术语基准）。
- 作用域：用户级 disable-model-invocation: true（全局手动 /video-notes）+ Obsidian vault 项目级薄壳（vault 内自动触发）。
- 验证：三脚本 py_compile 通过；annotate.py 用 P24 现存源帧复刻 2 张 v3 成品比对一致；extract_frames.py 三模式实测。
