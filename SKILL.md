---
name: video-notes
description: 把教学/操作类视频完整复现为"标注截图 + 书面讲解"的图文笔记（B站课程逐集笔记、软件实操教程、录屏演示皆适用）。目标是"看笔记 ≈ 看视频"：操作零遗漏、参数零遗漏、可独立复现。用户说"总结这个视频/把这集做成笔记/图文复现/继续做下一集课程笔记"时必须使用本 skill。仅询问视频讲了什么（不要产出笔记）、或纯下载视频（用 cn-video-downloader）时不要触发。
disable-model-invocation: true
---

# video-notes：视频 → 图文笔记流水线

把一集视频加工成图文笔记：关键帧截图（红框 + 红底白字标签智能标注）配书面语讲解，落库为 Obsidian 笔记或用户指定目录的 Markdown。流水线在 Fluent 课程 P24 上经三轮人工验收定型，样式与工序不要凭感觉偏离；确需偏离时向用户说明理由。

## 作用范围与前置确认

- 本 skill 全局默认**只能用户手动 `/video-notes` 调用**；在 Obsidian vault 项目内由项目级同名薄壳提供自动触发。
- 开工前确认三件事：**哪一集/哪个视频**（多 P 合集必须明确分 P 号）、**输出位置**（vault 会话默认按 vault 规范落库；否则询问输出目录）、**详略档位**（正课全量图文；选修/答疑类默认降级为要点摘要，除非用户点名全量）。
- **落 vault 但会话不在 vault 内时，先补规范**：凡最终产物要落进 Obsidian vault，而当前会话的 cwd 不在 vault 内（即 vault 根 `CLAUDE.md` 未被自动加载）——动笔前先完整读一遍 vault 根 `CLAUDE.md`，再按 ⑥ 的 vault 分支落库。那里才有 frontmatter 硬约束、目录归位表、`log.md` 四级折叠规则、`raw/` 只读纪律等只存在于 vault 侧的规范，`pipeline.md` ⑥ 只列了落点路径，不含这些。
- 按需逐集处理，绝不批量预下载/预处理整个合集。

## 六阶段流水线（详细操作见 references/pipeline.md）

1. **获取**：下载单集视频（B站走 cn-video-downloader skill，多 P 带 `?p=N` 并核对产物文件名）。
2. **文字轨**：平台/AI 字幕优先（B站 `opencli bilibili subtitle <BV> --page N`），无字幕才走音频转写兜底；产物为带时间轴转写稿，通读后搭章节骨架 + 列目标帧时间戳清单（outline）。
3. **视觉轨**：`scripts/extract_frames.py` 三模式抽帧——场景检测打底、按 outline 定点补抽、固定间隔兜底；候选帧先用 `scripts/contact_sheet.py` 拼总览粗筛，再精读入选帧。
4. **理解对齐**：判定视频类型（操作型/理论型/问答型）定组织轴；转写稿与帧按时间戳对齐；**界面文字是术语基准**，用帧校正字幕错词。
5. **制图**：读帧标定红框坐标 → 写 JSON 配置 → `scripts/annotate.py` 渲染 → 按防呆清单核验（见 references/annotate-style.md，含样式规范全文）。
6. **落库校验**：写笔记页 + 图片入库 + 索引更新 + 完整性对账（拿转写稿反查操作/参数是否全覆盖）+ 链接与嵌图校验。

## 质量红线（每条都有真实翻车史，不要试探）

- 图片全程保持**原始分辨率**，任何环节不缩放成品（缩放导致模糊返工过一次）。
- **高信息密度图不拼图**，拆单图各配讲解；只有低密度对比图可以并排。
- 标注用 v3 定稿样式：细红框 + 红底白字标签 + 细引线，标签自动落空白区，不扩画布，允许轻微遮挡换排版整齐。
- **对话框/弹窗类、以及标签引用了图内数值的关键帧，渲染后必须全分辨率逐张核验**"红框-内容一致性"——定点抽帧既会撞 UI 过渡瞬间（3 次中招），也会抽到输入框里的拖动中间值而非讲师键入的目标值（2 次中招）；缩略总览一律查不出来。
- **只标讲师讲过的**：每条标签所指的对象，必须能在转写稿里找到对应讲解。界面里存在、但视频只字未提的东西（尤其是恰好在帧里可见、又恰好占着大片空白好放标签的左侧选项面板）**一律不标**——红框会被读者默认为"讲师强调过的要点"，标了等于凭空捏造重点。确有价值的旁支写进正文，并显式注明「视频未展开讲解」。
- 讲解**零遗漏但书面语重构**：每个操作、每个参数值、讲师强调的坑都要进笔记；不逐字转录口语。
- **不罗列字幕转写错误**：AI 字幕认错词是既定前提，读者按上下文自己就能还原，专门列「术语校正表」或「字幕坑」callout 属于噪音。笔记**直接用正确写法行文**即可，不留纠错痕迹。唯一例外是**讲师口述与界面文字确实不一致**（讲师真说了那个词，不是转写错）——这种会让人跟着操作时找不到按钮，要保留提示，但措辞按"讲解与界面不一致"写，不要写成字幕问题。
- 产出仅限用户个人学习使用，不作为对外发布素材。

## 自我优化机制（本 skill 的核心设计）

skill 要越用越准，靠三层闭环：

1. **卡点即记**：运行中出现以下任一情况，当场把"日期 + 场景 + 现象 + 处置"追加进 `references/lessons.md`（追加不需用户批准）：
   - 同一步骤失败重试 ≥2 次；
   - 用户指出成品缺陷；
   - 遇到新平台、新软件 UI、新视频类型带来的流程差异。
2. **每集微复盘**：单集收尾汇报时附一节"本集卡点与优化建议"——列出新增 lessons，并主动向用户提 1~3 个**具体可选**的优化项（如抽帧阈值调整、图密度增减、标签措辞风格），由用户拍板。
3. **教训泛化入本体**：某条 lesson 反复出现或明显可泛化时，向用户提议修改 SKILL.md / references / scripts 本体；**获准后**修改，更新 CHANGELOG.md，并提醒用户去 cc-switch App 同步。

开工前先扫一遍 `references/lessons.md`，别再踩已记录的坑。

## 资源纪律

- 大头成本是精读候选帧：先总览粗筛，控制精读数量；单集预算参考 P24 实测（67 min 视频 ≈ 30 张成图、全程含三轮返工约 40 万 token，正常单轮 10~15 万）。
- 中间产物（视频/源帧/配置）在格式迭代期**保留**备重跑；用户明确说定稿后，处理完默认删视频留源帧。
- 报告文件大小一律用 MB/GB。
